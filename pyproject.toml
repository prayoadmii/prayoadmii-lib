[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "prayoadmii-lib"
version = "0.1.2"
description = "A Python Library To Help Me Developing Things Faster"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "colorama"
]
license = {text = "MIT"}
authors = [
    {name = "PrayoadMii"}
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]